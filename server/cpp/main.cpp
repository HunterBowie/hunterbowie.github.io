#include <iostream>
using namespace std;

int main() {
    int counter;
    cin >> counter;      // Read from standard input
    counter += 1;        // Add one
    cout << counter;     // Output to standard output
    return 0;
}
